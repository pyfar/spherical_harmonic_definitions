name: spherical_harmonic_definitions
channels:
  - conda-forge
dependencies:
  - jupyterlab
  - matplotlib
  - numpy
  - scipy=1.15
  - sympy
  - pip
  - python=3.12
  - ipykernel
  - ipython
  - jupyter-cache
  - pip:
    - spharpy<1.0.0
prefix: ./env/spherical_harmonic_definitions
